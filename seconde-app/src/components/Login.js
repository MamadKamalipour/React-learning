import React from 'react';
import useTitle from "../hooks/useTitle"
const Login = () => {

    useTitle("login page")

    return (
        
        <div>
           <h1>Login Page</h1> 
        </div>
    );
};

export default Login;